{
  "name": "openclaw-secure-kit",
  "version": "0.1.0",
  "private": true,
  "description": "Phase 0 scaffold for the openclaw-secure-kit CLI",
  "bin": {
    "ocs": "dist/ocs.js"
  },
  "scripts": {
    "lint": "tsc --noEmit -p tsconfig.json",
    "test": "node --test test/**/*.test.js",
    "build": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "yaml": "^2.8.1",
    "zod": "^3.24.1"
  }
}
